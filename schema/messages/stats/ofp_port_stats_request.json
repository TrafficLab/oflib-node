/*
 * Author: Zoltán Lajos Kis <zoltan.lajos.kis@ericsson.com>
 */

{
    "id" : "ofp_port_stats_request",
    "type" : "object",
    "extends" : {"$ref" : "./ofp_stats_request.json"},  /* OFPST_PORT */
    "properties" : [
        "port_no" : {        /* OFPST_PORT message must request statistics
                              * either for a single port or for all ports. */
            "extends" : {"$ref" : "../../types/port_id.json"},
            "description" : "if not present, request for all ports"
        }
    ]
}
