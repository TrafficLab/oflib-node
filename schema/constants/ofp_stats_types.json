/*
 * Author: Zoltán Lajos Kis <zoltan.lajos.kis@ericsson.com>
 */

{
    "id" : "ofp_stats_types",
    "type" : "string",
    "enum" : [
        "OFPST_DESC",          /* Description of this OpenFlow switch. */
        "OFPST_FLOW",          /* Individual flow statistics. */
        "OFPST_AGGREGATE",     /* Aggregate flow statistics. */
        "OFPST_TABLE",         /* Flow table statistics. */
        "OFPST_PORT",          /* Port statistics. */
        "OFPST_QUEUE",         /* Queue statistics for a port */
        "OFPST_GROUP",         /* Group counter statistics. */
        "OFPST_GROUP_DESC",    /* Group description statistics. */
        "OFPST_EXPERIMENTER"   /* Experimenter extension. */
    ]
}
