/*
 * Author: Zoltán Lajos Kis <zoltan.lajos.kis@ericsson.com>
 */

{
    "id" : "ofp_table_config",
    "type" : "string",
    "enum" : [
        "OFPTC_TABLE_MISS_CONTROLLER",     /* Send to controller. */
        "OFPTC_TABLE_MISS_CONTINUE",       /* Continue to the next table in the
                                              pipeline (OpenFlow 1.0 behavior). */
        "OFPTC_TABLE_MISS_DROP"            /* Drop the packet. */
    ]
}
